custom-ui/node_modules/

build/
custom-ui/build/

# Локальный исходник Word — не в черновом репозитории
strongSwan.docx

# Входящие черновики (DOCX и т.д.) — только локально, не в git
incoming/